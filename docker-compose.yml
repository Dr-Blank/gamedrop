services:
  gamedrop:
    image: ghcr.io/dr-blank/gamedrop:latest
    restart: unless-stopped
    environment:
      - TZ=${TZ:-UTC}
    ports:
      - "8765:8000"
    volumes:
      - ./gamedrop-data:/data
